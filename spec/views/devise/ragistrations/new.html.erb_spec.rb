# require 'rails_helper'

# RSpec.describe "devise/registrations/new", type: :view do

#   before do
#     @resource = User.new
#     @user = resource
#     @resource_name = 'user'
#     render
#   end



#   it "have a sign up forms" do
#   expect(rendered).to match /Sign up/
#   expect(rendered).to match /メールアドレス/
#   expect(rendered).to match /ユーザーネーム/
#   expect(rendered).to match /自己紹介/
#   expect(rendered).to match /お住まい/
#   expect(rendered).to match /パスワード/
#   expect(rendered).to match /パスワード（確認用）/
#   end
# end
<% if current_user.present? && !current_user.guest? && current_user != @user && current_user.following?(@user) && @user.following?(current_user) %>
  <%= link_to chat_path(@user.id), class: "ml-3" do %>
    <i class="fab fa-telegram-plane fa-lg"></i>
  <% end %>
<% end %>

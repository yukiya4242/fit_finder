<% if @error_message.present? %>
  alert("<%= @error_message %>")
<% else %>
  if ($("#comments").length > 0) {
    $("#comments").prepend("<%= j(render('posts/comment', comment: @comment)) %>")
  }
<% end %>

<p>
  <i class="far fa-heart <%= 'active' if current_user.liked_posts.include?(post) %>"></i>
  <span class="like-count"><%= post.likes.count %></span>
</p>

<%= form_with(model: [post, post.likes.build], local: true) do |f| %>
  <%= f.submit '', class: 'd-none' %>
<% end %>
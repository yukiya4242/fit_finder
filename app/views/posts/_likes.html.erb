<p>いいね数: <%= post.likes.count %></p>

<% if current_user.liked_posts.include?(post) %>
  <%= button_to "いいね解除", post_likes_path(post, post.likes.find_by(user_id: current_user.id)), method: :delete %>
<% else %>
  <%= button_to "いいね", post_likes_path(post), method: :post %>
<% end %>
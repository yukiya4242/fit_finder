<h1>ユーザー検索</h1>

<%= render "search" %>

<!--もしキーワードが入力されてそのユーザーがいたら-->
<% if @users.present? %> 
<ul>
  <% @users.each do |user| %>
  <li>
    <%= link_to user.username, user_path(user) %>
  </li>
  <% end %>
</ul>

<!--いなかったら-->
<% else %>
<p>該当するユーザーはいません</p>
<% end %>
<h2>
  <%= @user.username %>の
  <% if action_name == 'following' %>
  フォロー中
  <% else %>
  フォロワー
  <% end %>
</h2>

<ul>
  <% @users.each do |user| %>
  <li>
    <%= link_to user.username, user_path(user) %>
  </li>
  <% end %>
</ul>